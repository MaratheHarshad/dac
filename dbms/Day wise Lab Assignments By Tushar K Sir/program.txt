create function firstClass(marks int)
varchar(15)
deterministic
begin
declare result varchar(15);
if marks > 40 then
  SET result = 'YES';
elseif (marks <= 40) then
  SET result = 'NO';
end if;
return (result);
end &&








CREATE FUNCTION Customer_Occupation(
 age int
 )
 RETURNS VARCHAR(20)
 DETERMINISTIC
 BEGIN
 DECLARE customer_occupation VARCHAR(20);
 IF age > 35 THEN
 SET customer_occupation = 'Scientist';
 ELSEIF (age <= 35 AND
 age >= 30) THEN
 SET customer_occupation = 'Engineer';
 ELSEIF age < 30 THEN
 SET customer_occupation = 'Actor';
 END IF;
 -- return the customer occupation
 RETURN (customer_occupation);
 END$$ 
